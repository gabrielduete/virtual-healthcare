import { render } from '@testing-library/react'

import SectionServices from '.'

describe('<SectionServices />', () => {
  beforeEach(() => {
    render(<SectionServices />)
  })

  it.todo('should render title and subtitle correctly')

  it.todo('should render button correctly')

  it.todo('should render services items correctly')
})
